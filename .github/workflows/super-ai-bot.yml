name: Super AI Project & Android Bot

on:
  issues:
    types: [opened, edited]

jobs:
  ai-bot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python & Tools
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: |
          python -m pip install --upgrade pip openai

      - name: Super AI Bot - Natural Language, Project, Android, Everything
        env:
          OPENAI_API_KEY: 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
          GH_USER: ${{ github.event.issue.user.login }}
          GH_OWNER: ${{ github.repository_owner }}
        run: |
          # ... (كل كود البايثون كما هو) ...
          # اترك باقي الكود كما هو
          
      - name: Commit and Push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --staged --quiet || (git commit -m "Super AI Bot: auto changes")
          git pull origin ${{ github.ref_name }} --rebase || true
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }} HEAD:${{ github.ref_name }} || echo "No changes to push"

      - name: Add comment to issue
        if: always()
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.issue.number }}
          body-path: results.log
